// @generated by tests/doc_examples.rs — do not edit.
//
// Every Rust example in the embedder-facing docs, wrapped so it can be
// compiled. Cargo builds this as a test target, so an example that does
// not typecheck fails the build. See tests/doc_examples.rs for why.
//
// Regenerate: DBD_REGEN_DOC_EXAMPLES=1 cargo test --test doc_examples
//
// docs-digest: 0xb38960ddb587f49f
#![allow(dead_code, unused_imports, non_snake_case)]

// README.md — block 0
#[allow(unused, clippy::all)]
async fn doc_readme_md_0() -> dbd_core::Result<()> {
    #[allow(unused_variables)]
    let sql: &str = "";
    #[allow(unused_variables)]
    let database_url: &str = "";
    #[allow(unused_variables)]
    let db_url: &str = "";
    #[allow(unused_variables)]
    let db_version: u32 = 0;
    #[allow(unused_variables)]
    let entities: Vec<dbd_core::Entity> = Vec::new();
    #[allow(unused_variables)]
    let project_dir = std::path::Path::new(".");
    #[allow(unused_variables)]
    let path = std::path::Path::new("design.yaml");
    use dbd_core::Design;
    use dbd_core::adapter::postgres::PostgresAdapter;
    use dbd_core::design::Progress;
    use std::path::Path;

    async fn run_migrations(database_url: &str) -> anyhow::Result<()> {
        let design = Design::from_config(Path::new("database/design.yaml"), "prod")?;

        let adapter = PostgresAdapter::new(database_url, &design.config().project.name).await?;

        // apply(adapter, name, dry_run, scope, progress)
        let scope = design.resolve_scope(None, None)?;
        design
            .apply(&adapter, None, false, Some(&scope), Progress::none())
            .await?;
        Ok(())
    }
    Ok(())
}

// docs/skills/dbd/SKILL.md — block 0
#[allow(unused, clippy::all)]
async fn doc_docs_skills_dbd_skill_md_0() -> dbd_core::Result<()> {
    #[allow(unused_variables)]
    let sql: &str = "";
    #[allow(unused_variables)]
    let database_url: &str = "";
    #[allow(unused_variables)]
    let db_url: &str = "";
    #[allow(unused_variables)]
    let db_version: u32 = 0;
    #[allow(unused_variables)]
    let entities: Vec<dbd_core::Entity> = Vec::new();
    #[allow(unused_variables)]
    let project_dir = std::path::Path::new(".");
    #[allow(unused_variables)]
    let path = std::path::Path::new("design.yaml");
    use dbd_core::design::Progress;
    use dbd_core::{ApplyComplete, Design, connect};
    use std::path::Path;

    // 1. Load the declarative design for an environment (sync; scans ddl/ next to the config).
    let mut design = Design::from_config(Path::new("design.yaml"), "prod")?;

    // 2. Validate offline — no DB needed.
    let report = design.report(None, None);
    //    report.issues              → errors blocking the active scope (what apply refuses on)
    //    report.out_of_scope_issues → errored entities the scope excludes (non-blocking)
    //    report.warnings            → entities with warnings (e.g. unresolved entity refs)

    // 3. Connect an adapter — Postgres/SQLite/Convex chosen from the URL scheme.
    let adapter = connect("postgres://localhost/mydb", &design.config().project.name).await?;

    // 4. Apply schema + entities + pending migrations.
    //    The three callbacks travel together in ONE `Progress` value — they are not
    //    three arguments. `Progress::none()` for a silent run.
    let scope = design.resolve_scope(None, None)?; // None,None ⇒ full design, default deps policy
    design
        .apply(
            &*adapter,
            None,
            /*dry_run*/ false,
            Some(&scope),
            Progress {
                on_start: |desc: &str| println!("→ {desc}"),
                on_done: |desc: &str, err: Option<&str>| {
                    if let Some(e) = err {
                        eprintln!("✗ {desc}: {e}")
                    }
                },
                on_complete: |s: ApplyComplete| println!("applied {} entities", s.applied),
            },
        )
        .await?;

    // Or apply + import + policies in one call — the same pipeline `dbd deploy` runs:
    design
        .deploy(&*adapter, false, Some(&scope), |s| {
            println!("deployed to v{}", s.apply.to_version);
            println!("{} table(s) imported", s.import.tables);
            // Non-fatal diagnostics: skipped imports and failed policy files.
            for w in s.warnings() {
                eprintln!("warning: {w}");
            }
        })
        .await?;
    Ok(())
}

// docs/skills/dbd/SKILL.md — block 1
#[allow(unused, clippy::all)]
async fn doc_docs_skills_dbd_skill_md_1() -> dbd_core::Result<()> {
    #[allow(unused_variables)]
    let sql: &str = "";
    #[allow(unused_variables)]
    let database_url: &str = "";
    #[allow(unused_variables)]
    let db_url: &str = "";
    #[allow(unused_variables)]
    let db_version: u32 = 0;
    #[allow(unused_variables)]
    let entities: Vec<dbd_core::Entity> = Vec::new();
    #[allow(unused_variables)]
    let project_dir = std::path::Path::new(".");
    #[allow(unused_variables)]
    let path = std::path::Path::new("design.yaml");
    let parsed = dbd_core::parser::parse_sql(sql)?; // ParsedFile
    for e in &parsed.entities {
        // e.entity_type / e.schema / e.name  — read off the CREATE statement
        // e.refers, e.references             — typed edges (FK, view dep, function call)
        // e.reads, e.writes                  — separated, for functions and procedures
    }
    Ok(())
}

// docs/skills/dbd/SKILL.md — block 2
#[allow(unused, clippy::all)]
async fn doc_docs_skills_dbd_skill_md_2() -> dbd_core::Result<()> {
    #[allow(unused_variables)]
    let sql: &str = "";
    #[allow(unused_variables)]
    let database_url: &str = "";
    #[allow(unused_variables)]
    let db_url: &str = "";
    #[allow(unused_variables)]
    let db_version: u32 = 0;
    #[allow(unused_variables)]
    let entities: Vec<dbd_core::Entity> = Vec::new();
    #[allow(unused_variables)]
    let project_dir = std::path::Path::new(".");
    #[allow(unused_variables)]
    let path = std::path::Path::new("design.yaml");
    use dbd_core::parser::{Dialect, parse_sql_as};

    let parsed = parse_sql_as(Dialect::detect(sql), sql)?;
    println!("{:?} file, read as {:?}", parsed.kind, parsed.dialect);
    Ok(())
}

// docs/skills/dbd/SKILL.md — block 3
#[allow(unused, clippy::all)]
async fn doc_docs_skills_dbd_skill_md_3() -> dbd_core::Result<()> {
    #[allow(unused_variables)]
    let sql: &str = "";
    #[allow(unused_variables)]
    let database_url: &str = "";
    #[allow(unused_variables)]
    let db_url: &str = "";
    #[allow(unused_variables)]
    let db_version: u32 = 0;
    #[allow(unused_variables)]
    let entities: Vec<dbd_core::Entity> = Vec::new();
    #[allow(unused_variables)]
    let project_dir = std::path::Path::new(".");
    #[allow(unused_variables)]
    let path = std::path::Path::new("design.yaml");
    use dbd_core::parser::{Dialect, parse_sql_as};

    let parsed = parse_sql_as(Dialect::TSql, sql)?;
    for e in &parsed.entities {
        println!("{} reads {:?}", e.name, e.reads); // owned by a declaration
    }
    println!("the file itself reads {:?}", parsed.references.reads);
    println!("        writes {:?}", parsed.references.writes);
    println!("        calls  {:?}", parsed.references.calls);
    Ok(())
}

// src/assets/skills/dbd/SKILL.md — block 0
#[allow(unused, clippy::all)]
async fn doc_src_assets_skills_dbd_skill_md_0() -> dbd_core::Result<()> {
    #[allow(unused_variables)]
    let sql: &str = "";
    #[allow(unused_variables)]
    let database_url: &str = "";
    #[allow(unused_variables)]
    let db_url: &str = "";
    #[allow(unused_variables)]
    let db_version: u32 = 0;
    #[allow(unused_variables)]
    let entities: Vec<dbd_core::Entity> = Vec::new();
    #[allow(unused_variables)]
    let project_dir = std::path::Path::new(".");
    #[allow(unused_variables)]
    let path = std::path::Path::new("design.yaml");
    use dbd_core::design::Progress;
    use dbd_core::{ApplyComplete, Design, connect};
    use std::path::Path;

    // 1. Load the declarative design for an environment (sync; scans ddl/ next to the config).
    let mut design = Design::from_config(Path::new("design.yaml"), "prod")?;

    // 2. Validate offline — no DB needed.
    let report = design.report(None, None);
    //    report.issues              → errors blocking the active scope (what apply refuses on)
    //    report.out_of_scope_issues → errored entities the scope excludes (non-blocking)
    //    report.warnings            → entities with warnings (e.g. unresolved entity refs)

    // 3. Connect an adapter — Postgres/SQLite/Convex chosen from the URL scheme.
    let adapter = connect("postgres://localhost/mydb", &design.config().project.name).await?;

    // 4. Apply schema + entities + pending migrations.
    //    The three callbacks travel together in ONE `Progress` value — they are not
    //    three arguments. `Progress::none()` for a silent run.
    let scope = design.resolve_scope(None, None)?; // None,None ⇒ full design, default deps policy
    design
        .apply(
            &*adapter,
            None,
            /*dry_run*/ false,
            Some(&scope),
            Progress {
                on_start: |desc: &str| println!("→ {desc}"),
                on_done: |desc: &str, err: Option<&str>| {
                    if let Some(e) = err {
                        eprintln!("✗ {desc}: {e}")
                    }
                },
                on_complete: |s: ApplyComplete| println!("applied {} entities", s.applied),
            },
        )
        .await?;

    // Or apply + import + policies in one call — the same pipeline `dbd deploy` runs:
    design
        .deploy(&*adapter, false, Some(&scope), |s| {
            println!("deployed to v{}", s.apply.to_version);
            println!("{} table(s) imported", s.import.tables);
            // Non-fatal diagnostics: skipped imports and failed policy files.
            for w in s.warnings() {
                eprintln!("warning: {w}");
            }
        })
        .await?;
    Ok(())
}

// src/assets/skills/dbd/SKILL.md — block 1
#[allow(unused, clippy::all)]
async fn doc_src_assets_skills_dbd_skill_md_1() -> dbd_core::Result<()> {
    #[allow(unused_variables)]
    let sql: &str = "";
    #[allow(unused_variables)]
    let database_url: &str = "";
    #[allow(unused_variables)]
    let db_url: &str = "";
    #[allow(unused_variables)]
    let db_version: u32 = 0;
    #[allow(unused_variables)]
    let entities: Vec<dbd_core::Entity> = Vec::new();
    #[allow(unused_variables)]
    let project_dir = std::path::Path::new(".");
    #[allow(unused_variables)]
    let path = std::path::Path::new("design.yaml");
    let parsed = dbd_core::parser::parse_sql(sql)?; // ParsedFile
    for e in &parsed.entities {
        // e.entity_type / e.schema / e.name  — read off the CREATE statement
        // e.refers, e.references             — typed edges (FK, view dep, function call)
        // e.reads, e.writes                  — separated, for functions and procedures
    }
    Ok(())
}

// src/assets/skills/dbd/SKILL.md — block 2
#[allow(unused, clippy::all)]
async fn doc_src_assets_skills_dbd_skill_md_2() -> dbd_core::Result<()> {
    #[allow(unused_variables)]
    let sql: &str = "";
    #[allow(unused_variables)]
    let database_url: &str = "";
    #[allow(unused_variables)]
    let db_url: &str = "";
    #[allow(unused_variables)]
    let db_version: u32 = 0;
    #[allow(unused_variables)]
    let entities: Vec<dbd_core::Entity> = Vec::new();
    #[allow(unused_variables)]
    let project_dir = std::path::Path::new(".");
    #[allow(unused_variables)]
    let path = std::path::Path::new("design.yaml");
    use dbd_core::parser::{Dialect, parse_sql_as};

    let parsed = parse_sql_as(Dialect::detect(sql), sql)?;
    println!("{:?} file, read as {:?}", parsed.kind, parsed.dialect);
    Ok(())
}

// src/assets/skills/dbd/SKILL.md — block 3
#[allow(unused, clippy::all)]
async fn doc_src_assets_skills_dbd_skill_md_3() -> dbd_core::Result<()> {
    #[allow(unused_variables)]
    let sql: &str = "";
    #[allow(unused_variables)]
    let database_url: &str = "";
    #[allow(unused_variables)]
    let db_url: &str = "";
    #[allow(unused_variables)]
    let db_version: u32 = 0;
    #[allow(unused_variables)]
    let entities: Vec<dbd_core::Entity> = Vec::new();
    #[allow(unused_variables)]
    let project_dir = std::path::Path::new(".");
    #[allow(unused_variables)]
    let path = std::path::Path::new("design.yaml");
    use dbd_core::parser::{Dialect, parse_sql_as};

    let parsed = parse_sql_as(Dialect::TSql, sql)?;
    for e in &parsed.entities {
        println!("{} reads {:?}", e.name, e.reads); // owned by a declaration
    }
    println!("the file itself reads {:?}", parsed.references.reads);
    println!("        writes {:?}", parsed.references.writes);
    println!("        calls  {:?}", parsed.references.calls);
    Ok(())
}

// docs/llms/llms-full.txt — block 0
#[allow(unused, clippy::all)]
async fn doc_docs_llms_llms_full_txt_0() -> dbd_core::Result<()> {
    #[allow(unused_variables)]
    let sql: &str = "";
    #[allow(unused_variables)]
    let database_url: &str = "";
    #[allow(unused_variables)]
    let db_url: &str = "";
    #[allow(unused_variables)]
    let db_version: u32 = 0;
    #[allow(unused_variables)]
    let entities: Vec<dbd_core::Entity> = Vec::new();
    #[allow(unused_variables)]
    let project_dir = std::path::Path::new(".");
    #[allow(unused_variables)]
    let path = std::path::Path::new("design.yaml");
    use dbd_core::design::Progress;
    use dbd_core::{ApplyComplete, Design, connect};
    use std::path::Path;

    // Load the declarative design for an environment (sync; scans ddl/ next to the config).
    let mut design = Design::from_config(Path::new("design.yaml"), "prod")?;

    // Inspection — offline, no database. (`report` takes &mut self: it caches validation.)
    let report = design.report(None, None);
    // report.issues:              errors blocking the active scope (what apply refuses on)
    // report.out_of_scope_issues: errored entities the scope excludes (non-blocking)
    // report.warnings:            entities with warnings
    // report.gaps:                out-of-scope dependency gaps (when a scope is passed)

    // Connect an adapter — Postgres/SQLite/Convex is chosen from the URL scheme.
    let adapter = connect("postgres://localhost/mydb", &design.config().project.name).await?;

    // Resolve the scope once (None, None ⇒ full design, the scope's default deps policy).
    let scope = design.resolve_scope(None, None)?;

    // apply / import_data take FIVE arguments: (&*adapter, name, dry_run, scope,
    // progress). The three callbacks travel together in one `Progress` value —
    // on_start(&str), on_done(&str, Option<&str>), on_complete(summary) — they are
    // NOT three separate arguments. `Progress::none()` for a silent run.
    // deploy drops the name and takes on_complete directly: (&*adapter, dry_run,
    // scope, on_complete(DeployComplete)).
    design
        .apply(
            &*adapter,
            None,
            /*dry_run*/ false,
            Some(&scope),
            Progress {
                on_start: |desc: &str| println!("→ {desc}"),
                on_done: |desc: &str, err: Option<&str>| {
                    if let Some(e) = err {
                        eprintln!("✗ {desc}: {e}")
                    }
                },
                on_complete: |s: ApplyComplete| println!("applied {} entities", s.applied),
            },
        )
        .await?;
    design
        .import_data(&*adapter, None, false, Some(&scope), Progress::none())
        .await?;

    // Or apply + import + policies in one call — the same pipeline `dbd deploy`
    // runs, so an embedder and the CLI cannot drift. A failed policy file does not
    // fail the deploy; it lands in s.policies.failed and in s.warnings().
    design
        .deploy(&*adapter, false, Some(&scope), |s| {
            println!("v{} — {} table(s) imported", s.apply.to_version, s.import.tables);
            for w in s.warnings() {
                eprintln!("warning: {w}");
            }
        })
        .await?;
    // Use design.deploy_with_progress(&*adapter, dry_run, scope, Progress { .. })
    // for per-step callbacks (spinner / logging).

    // Dependency graph (for visualization) — returns Result.
    let graph = design.graph(None, None)?;
    // graph.nodes, graph.edges, graph.layers
    Ok(())
}

// docs/design/architecture.md — block 0
#[allow(unused, clippy::all)]
async fn doc_docs_design_architecture_md_0() -> dbd_core::Result<()> {
    #[allow(unused_variables)]
    let sql: &str = "";
    #[allow(unused_variables)]
    let database_url: &str = "";
    #[allow(unused_variables)]
    let db_url: &str = "";
    #[allow(unused_variables)]
    let db_version: u32 = 0;
    #[allow(unused_variables)]
    let entities: Vec<dbd_core::Entity> = Vec::new();
    #[allow(unused_variables)]
    let project_dir = std::path::Path::new(".");
    #[allow(unused_variables)]
    let path = std::path::Path::new("design.yaml");
    // High-level: one-call operations (deploy, apply, inspect)
    use dbd_core::Design;
    use dbd_core::design::Progress;
    use std::path::Path;

    let mut design = Design::from_config(Path::new("design.yaml"), "prod")?; // sync; scans ddl/
    let adapter = dbd_core::connect(db_url, &design.config().project.name).await?;
    let scope = design.resolve_scope(None, None)?;
    // apply/import_data: (&*adapter, name, dry_run, scope, progress)
    // The three callbacks travel together in one `Progress`; `Progress::none()` is silent.
    design
        .apply(&*adapter, None, false, Some(&scope), Progress::none())
        .await?; // entities + migrations
    design
        .import_data(&*adapter, None, false, Some(&scope), Progress::none())
        .await?; // staging data
    let report = design.report(None, None); // inspect: errors/warnings

    // Mid-level: individual subsystems
    use dbd_core::config;
    use dbd_core::dependency;
    use dbd_core::snapshot;

    let config = config::read(Path::new("design.yaml"))?;
    let sorted = dependency::sort_by_dependencies(&entities);
    let pending = snapshot::pending_migrations(db_version, Path::new(".")); // -> Vec<PendingMigration>

    // Low-level: parser, adapter trait, entity types
    use dbd_core::adapter::DatabaseAdapter;
    use dbd_core::entity::{Entity, EntityType};
    use dbd_core::parser::parse_entity; // parse one DDL file's SQL → Entity (identity from the PATH)
    use dbd_core::parser::parse_sql; // parse any SQL → ParsedFile (identity from the STATEMENTS)
    Ok(())
}

// docs/design/architecture.md — block 1
#[allow(unused, clippy::all)]
async fn doc_docs_design_architecture_md_1() -> dbd_core::Result<()> {
    #[allow(unused_variables)]
    let sql: &str = "";
    #[allow(unused_variables)]
    let database_url: &str = "";
    #[allow(unused_variables)]
    let db_url: &str = "";
    #[allow(unused_variables)]
    let db_version: u32 = 0;
    #[allow(unused_variables)]
    let entities: Vec<dbd_core::Entity> = Vec::new();
    #[allow(unused_variables)]
    let project_dir = std::path::Path::new(".");
    #[allow(unused_variables)]
    let path = std::path::Path::new("design.yaml");
    let parsed = dbd_core::parser::parse_sql(sql)?; // ParsedFile
    for e in &parsed.entities {
        // e.entity_type / e.schema / e.name  — from the CREATE, not a path
        // e.refers, e.references             — typed edges (FK, view dep, function call)
        // e.reads, e.writes                  — separated, for routines
    }
    Ok(())
}

// docs/design/architecture.md — block 2
#[allow(unused, clippy::all)]
async fn doc_docs_design_architecture_md_2() -> dbd_core::Result<()> {
    #[allow(unused_variables)]
    let sql: &str = "";
    #[allow(unused_variables)]
    let database_url: &str = "";
    #[allow(unused_variables)]
    let db_url: &str = "";
    #[allow(unused_variables)]
    let db_version: u32 = 0;
    #[allow(unused_variables)]
    let entities: Vec<dbd_core::Entity> = Vec::new();
    #[allow(unused_variables)]
    let project_dir = std::path::Path::new(".");
    #[allow(unused_variables)]
    let path = std::path::Path::new("design.yaml");
    // In a Rust web server's startup routine
    use dbd_core::Design;
    use dbd_core::design::Progress;
    use std::path::Path;

    async fn run_migrations(database_url: &str) -> dbd_core::Result<()> {
        let design = Design::from_config(Path::new("database/design.yaml"), "prod")?;
        let adapter = dbd_core::connect(database_url, &design.config().project.name).await?;
        let scope = design.resolve_scope(None, None)?;
        design
            .apply(&*adapter, None, false, Some(&scope), Progress::none())
            .await?;
        design
            .import_data(&*adapter, None, false, Some(&scope), Progress::none())
            .await?;
        Ok(())
    }
    Ok(())
}

// docs/design/architecture.md — block 3
#[allow(unused, clippy::all)]
async fn doc_docs_design_architecture_md_3() -> dbd_core::Result<()> {
    #[allow(unused_variables)]
    let sql: &str = "";
    #[allow(unused_variables)]
    let database_url: &str = "";
    #[allow(unused_variables)]
    let db_url: &str = "";
    #[allow(unused_variables)]
    let db_version: u32 = 0;
    #[allow(unused_variables)]
    let entities: Vec<dbd_core::Entity> = Vec::new();
    #[allow(unused_variables)]
    let project_dir = std::path::Path::new(".");
    #[allow(unused_variables)]
    let path = std::path::Path::new("design.yaml");
    use dbd_core::Design;
    use dbd_core::dependency::GraphResult;
    use std::path::Path;

    let mut design = Design::from_config(Path::new("design.yaml"), "prod")?;

    // Get dependency graph for visualization
    let graph: GraphResult = design.graph(None, None)?;
    // graph.nodes: Vec<{name, type, schema}>
    // graph.edges: Vec<{from, to}>
    // graph.layers: Vec<Vec<String>>

    // Get validation report for dashboard
    let report = design.report(None, None);
    // report.issues: Vec<Entity>   (entities with errors)
    // report.warnings: Vec<Entity> (entities with warnings)

    // Get snapshot diff for migration preview
    use dbd_core::snapshot;
    let pending = snapshot::pending_migrations(db_version, project_dir); // -> Vec<PendingMigration>
    // Each migration: { from_version, to_version, altered, dropped }
    Ok(())
}
